<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chats</title>
</head>
<body>
  
    <h3>Edit chat: <%= chats._id %></h3>
    <p>This chat was sent from <b><%= chats.from %> to <%= chats.to %></b></p>
    <form method="POST" action="/chats/<%=chats._id%>?_method=PUT">
        <textarea name="msg" rows="10" cols="30"><%= chats.msg %></textarea>
        <button type="submit">Edit</button>
    </form>
</body>
</html>
